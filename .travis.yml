language: node_js
node_js:
  - 10

notifications:
  email: false
cache:
  directories:
    - node_modules
    - ~/.npm
    - ~/.cache

install:
  - yarn install

before_script:
  - yarn run build

script:
  - yarn run test